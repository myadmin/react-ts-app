const PageNotFound = ({ location }: { location?: any }) => {
    return (
        <p>
            Page not found - the path, did not match
            any React Router routes.
        </p>
    );
}

export default PageNotFound;